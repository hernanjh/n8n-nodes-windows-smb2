# Windows Fileshare SMB2 Test Configuration
# Copy this file to .env and update with your actual credentials
# Note: .env is in .gitignore to keep credentials secure

# Windows Fileshare UNC path (e.g., \\192.168.1.100\shared or \\SERVERNAME\ShareName)
SMB_SHARE=\\\\192.168.1.100\\shared

# Windows Domain (use WORKGROUP for non-domain computers)
SMB_DOMAIN=WORKGROUP

# Windows Username
SMB_USERNAME=testuser

# Windows Password
SMB_PASSWORD=testpassword

# Connection timeout in milliseconds (default: 30000)
SMB_TIMEOUT=30000

# Test directory path (will be created/cleaned up during tests)
# Use backslashes for Windows paths
SMB_TEST_DIR=\\test-n8n-smb2

# Optional: Test large file operations (slower, default: false)
# TEST_LARGE_FILE=true

# Optional: Keep test directory after tests complete (default: cleanup)
# CLEANUP_TEST_DIR=false
